<div class="container">
    <form action="/category" method="post">
        <input type="hidden" name="_csrf" value="<%= _csrf %>"/>
        <div>
            <div class="form-group">
                <label class="col-form-label-lg">Name</label>
                <input type="text" class="form-control" name="name" maxlength="20" placeholder="Name" value="<%= name %>" required>
            </div>
        </div>
        <button type="submit" class="blue-button mt-4" aria-label="Hinzufügen">Hinzufügen</button>
        <a class="blue-button mt-4" href="/category" aria-label="Zurück">Zurück</a>
    </form>
</div>